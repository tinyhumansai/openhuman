{
  "name": "openhuman-repo",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build": "yarn workspace app build",
    "compile": "yarn workspace app compile",
    "dev": "yarn workspace app dev",
    "format": "yarn workspace app format",
    "format:check": "yarn workspace app format:check",
    "lint": "yarn workspace app lint",
    "lint:fix": "yarn workspace app lint:fix",
    "tauri": "yarn workspace app tauri",
    "test": "yarn workspace app test",
    "test:coverage": "yarn workspace app test:coverage",
    "test:e2e": "yarn workspace app test:e2e",
    "test:e2e:all": "yarn workspace app test:e2e:all",
    "test:e2e:all:flows": "yarn workspace app test:e2e:all:flows",
    "test:e2e:build": "yarn workspace app test:e2e:build",
    "typecheck": "yarn workspace app compile"
  }
}
